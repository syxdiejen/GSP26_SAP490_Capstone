<mvc:View
    controllerName="zemail.template.app.controller.sign.UserSignatureObject"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:form="sap.ui.layout.form"
    xmlns:rte="sap.ui.richtexteditor">

    <f:DynamicPage id="signObjectPage">
        <f:title>
            <f:DynamicPageTitle>
                <f:heading>
                    <Title text="{viewState>/title}" level="H1" />
                </f:heading>

                <f:actions>
                    <Button
                        text="{i18n>publish}"
                        type="Accept"
                        icon="sap-icon://activate"
                        press=".onActivatePress" />

                    <Button
                        text="{i18n>saveAsDraft}"
                        type="Emphasized"
                        icon="sap-icon://save"
                        press=".onSavePress" />

                    <Button
                        text="{i18n>delete}"
                        type="Reject"
                        icon="sap-icon://delete"
                        press=".onDeletePress"
                        visible="{= ${viewState>/isCreate} === false }" />

                    <Button
                        text="{i18n>cancel}"
                        type="Transparent"
                        press=".onCancelPress" />
                </f:actions>
            </f:DynamicPageTitle>
        </f:title>

        <f:content>
            <VBox class="sapUiContentPadding">
                <Panel
                    headerText="{i18n>generalInformation}"
                    expandable="false"
                    class="sapUiResponsiveMargin">

                    <form:SimpleForm
                        editable="true"
                        layout="ResponsiveGridLayout"
                        labelSpanXL="3"
                        labelSpanL="3"
                        labelSpanM="4"
                        labelSpanS="12"
                        adjustLabelSpan="false"
                        emptySpanXL="4"
                        emptySpanL="4"
                        emptySpanM="0"
                        emptySpanS="0"
                        columnsXL="1"
                        columnsL="1"
                        columnsM="1">

                        <Label text="{i18n>signatureId}" />
                        <Input value="{SignId}" editable="false" />

                        <Label text="{i18n>signatureName}" required="true" />
                        <Input
                            id="inpSignName"
                            value="{SignName}"
                            placeholder="{i18n>enterSignatureName}"
                            liveChange=".onSignNameChange" />

                        <Label text="{i18n>defaultSignature}" />
                        <Switch
                            id="swDefault"
                            state="{IsDefault}"
                            customTextOn="{i18n>yes}"
                            customTextOff="{i18n>no}" />

                        <Label text="{i18n>owner}" />
                        <Input value="{OwnerId}" editable="false" />
                    </form:SimpleForm>
                </Panel>

                <Panel
                    headerText="{i18n>signatureContent}"
                    expandable="false"
                    class="sapUiResponsiveMargin sapUiSmallMarginTop">

                    <VBox class="sapUiSmallMargin">
                        <rte:RichTextEditor
                            id="signatureEditor"
                            width="100%"
                            height="350px"
                            value="{Content}"
                            editable="true"
                            sanitizeValue="false"
                            showGroupFont="true"
                            showGroupLink="true"
                            showGroupInsert="true"
                            showGroupStructure="true"
                            showGroupTextAlign="true" />
                    </VBox>
                </Panel>
            </VBox>
        </f:content>
    </f:DynamicPage>
</mvc:View>